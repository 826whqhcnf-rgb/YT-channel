{
  "name": "Faceless video pipeline",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt-get update -qq && sudo apt-get install -y ffmpeg && pip install -q -r requirements.txt",
  "customizations": {
    "vscode": {"extensions": ["ms-python.python"]}
  }
}
